const ExtUtilConfig = {
    getCollapsible(direction) {
        return {
            direction: direction,
            expandToolText: 'Развернуть',
            collapseToolText: 'Свернуть'
        }
    }
}